# OpenAI API Key (Required)
OPENAI_API_KEY=your-api-key-here

# SearxNG URL (Optional - for search examples)
SEARX_URL=http://localhost:58080

# Logging Configuration (Optional)
LOG_ENABLED=true

# Vector Store Selection (Optional - defaults to chromadb)
# Options: postgres, faiss, chromadb
VECTOR_DB=chromadb
